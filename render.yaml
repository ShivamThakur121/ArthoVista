services:
  - type: web
    name: artha-ventures
    env: node
    buildCommand: npm run build
    startCommand: node server.mjs
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: NODE_ENV
        value: production
